import preloader from './../../assets/preloader/preloader.svg';

const Preloader = () => {
    return (
        <div>
            <img src={ preloader } />
        </div>
    );
};

export default Preloader;